<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RAGIA Real Estate | راقية للعقارات - فخامة التسويق العقاري</title>
    <meta name="description" content="راقية للعقارات (RAGIA Real Estate) - خبرة 20 عاماً في التسويق العقاري الفاخر بالسودان، مصر، الإمارات، والسعودية. فلل، شقق، أبراج، واستثمارات عقارية.">
    <meta name="keywords" content="Ragia Real Estate, راقية للعقارات, عقارات السودان, عقارات الخرطوم, شقق تمليك, فلل فاخرة, استثمار عقاري, عقارات الإمارات, عقارات السعودية">
    
    <!-- Open Graph / Social Media SEO -->
    <meta property="og:title" content="RAGIA Real Estate | راقية للعقارات">
    <meta property="og:description" content="20 عاماً من التميز في التسويق العقاري الفاخر.">
    <meta property="og:type" content="website">
    
    <!-- Schema.org for Strong SEO -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "RealEstateAgent",
      "name": "Ragia Real Estate",
      "foundingDate": "2003",
      "telephone": "+249912339585",
      "areaServed": ["Sudan", "Egypt", "UAE", "Saudi Arabia"],
      "address": { "@type": "PostalAddress", "addressLocality": "Khartoum", "addressCountry": "SD" }
    }
    </script>

    <!-- Fonts & Icons -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@200;400;600;700;900&family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400&family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

    <style>
        /* =========================================
           1. ROOT VARIABLES & RESET (Luxury Theme)
           ========================================= */
        :root {
            --primary: #080b14; /* Deepest Space Black */
            --secondary: #d4af37; /* Royal Gold */
            --accent: #1a2238; /* Dark Navy */
            --light: #f8fafc;
            --grey: #94a3b8;
            --glass: rgba(255, 255, 255, 0.05);
            --glass-border: rgba(212, 175, 55, 0.2);
            --transition: all 0.6s cubic-bezier(0.25, 1, 0.5, 1);
            --perspective: 1000px;
        }

        [data-lang="en"] {
            --font-main: 'Poppins', sans-serif;
        }

        [data-lang="ar"] {
            --font-main: 'Cairo', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font-main);
            background-color: var(--primary);
            color: var(--light);
            line-height: 1.6;
            overflow-x: hidden;
            cursor: default;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: var(--primary); }
        ::-webkit-scrollbar-thumb { background: var(--secondary); border-radius: 10px; }

        h1, h2, h3, h4, .playfair {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
        }

        /* =========================================
           2. PRELOADER (Luxury Loading)
           ========================================= */
        #preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--primary);
            z-index: 10000;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            transition: opacity 0.8s ease, visibility 0.8s ease;
        }
        #preloader.hidden { opacity: 0; visibility: hidden; }
        .loader-logo {
            font-family: 'Playfair Display', serif;
            font-size: 4rem;
            color: var(--secondary);
            letter-spacing: 10px;
            animation: pulse 2s infinite;
        }
        .loader-span {
            font-size: 1rem;
            color: var(--light);
            letter-spacing: 5px;
            margin-top: 10px;
        }
        @keyframes pulse {
            0% { opacity: 0.5; transform: scale(0.95); }
            50% { opacity: 1; transform: scale(1.05); }
            100% { opacity: 0.5; transform: scale(0.95); }
        }

        /* =========================================
           3. FLYING LOGOS BACKGROUND (3D Effect)
           ========================================= */
        .bg-logos {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
            pointer-events: none;
        }
        .floating-logo {
            position: absolute;
            font-family: 'Playfair Display', serif;
            color: rgba(212, 175, 55, 0.03);
            font-size: 8rem;
            font-weight: 900;
            animation: floatLogo 20s linear infinite;
            text-transform: uppercase;
        }
        .floating-logo:nth-child(1) { top: 10%; left: 10%; animation-duration: 25s; }
        .floating-logo:nth-child(2) { top: 60%; left: 80%; font-size: 12rem; animation-duration: 35s; animation-direction: reverse; }
        .floating-logo:nth-child(3) { top: 80%; left: 20%; font-size: 6rem; animation-duration: 30s; }
        
        @keyframes floatLogo {
            0% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-100px) rotate(10deg); }
            100% { transform: translateY(0) rotate(0deg); }
        }

        /* =========================================
           4. NAVBAR (Glassmorphism & 3D Tilt)
           ========================================= */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            padding: 20px 8%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: var(--transition);
        }
        header.scrolled {
            background: rgba(8, 11, 20, 0.85);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--glass-border);
            padding: 15px 8%;
        }
        .logo-container {
            display: flex;
            flex-direction: column;
            cursor: pointer;
            transition: transform 0.3s ease;
        }
        .logo-container:hover { transform: scale(1.1) rotateX(10deg); }
        .logo-container h1 {
            color: var(--secondary);
            font-size: 1.8rem;
            letter-spacing: 2px;
            text-shadow: 0 0 15px rgba(212, 175, 55, 0.4);
        }
        .logo-container span {
            font-size: 0.6rem;
            letter-spacing: 6px;
            text-transform: uppercase;
            color: var(--light);
            text-align: center;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 40px;
        }
        .nav-links a {
            color: var(--light);
            text-decoration: none;
            font-weight: 600;
            font-size: 0.95rem;
            position: relative;
            transition: var(--transition);
        }
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--secondary);
            transition: var(--transition);
        }
        .nav-links a:hover { color: var(--secondary); }
        .nav-links a:hover::after { width: 100%; }

        .nav-controls {
            display: flex;
            align-items: center;
            gap: 20px;
        }
        .lang-btn {
            background: transparent;
            border: 1px solid var(--secondary);
            color: var(--secondary);
            padding: 8px 20px;
            border-radius: 30px;
            cursor: pointer;
            font-family: var(--font-main);
            font-weight: 700;
            font-size: 0.9rem;
            transition: var(--transition);
        }
        .lang-btn:hover {
            background: var(--secondary);
            color: var(--primary);
            box-shadow: 0 0 20px rgba(212, 175, 55, 0.5);
        }
        .menu-toggle {
            font-size: 1.8rem;
            cursor: pointer;
            color: var(--secondary);
            display: none;
        }

        /* =========================================
           5. HERO SECTION (Parallax & 3D Text)
           ========================================= */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            position: relative;
            overflow: hidden;
            perspective: var(--perspective);
        }
        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1600585154340-be6161a56a0c?q=80&w=2072&auto=format&fit=crop') no-repeat center center/cover;
            z-index: -2;
            animation: zoomBg 20s infinite alternate;
        }
        @keyframes zoomBg {
            from { transform: scale(1); }
            to { transform: scale(1.15); }
        }
        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, rgba(8,11,20,0.6), var(--primary));
            z-index: -1;
        }
        .hero-content {
            max-width: 900px;
            padding: 0 20px;
            transform-style: preserve-3d;
            animation: fadeUp 2s ease;
        }
        .hero-content h2 {
            font-size: 5rem;
            color: var(--secondary);
            margin-bottom: 20px;
            text-shadow: 0 10px 30px rgba(0,0,0,0.5);
            line-height: 1.1;
        }
        .hero-content p {
            font-size: 1.3rem;
            margin-bottom: 40px;
            color: #e2e8f0;
            font-weight: 300;
        }
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(80px) rotateX(10deg); }
            to { opacity: 1; transform: translateY(0) rotateX(0); }
        }
        .btn-gold {
            background: var(--secondary);
            color: var(--primary);
            padding: 18px 50px;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 800;
            cursor: pointer;
            text-decoration: none;
            transition: var(--transition);
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
        }
        .btn-gold::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(120deg, transparent, rgba(255,255,255,0.4), transparent);
            transition: 0.6s;
        }
        .btn-gold:hover::before { left: 100%; }
        .btn-gold:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 30px rgba(212, 175, 55, 0.4);
        }

        .scroll-down {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            color: var(--light);
            font-size: 2rem;
            animation: bounce 2s infinite;
        }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0) translateX(-50%); }
            40% { transform: translateY(-20px) translateX(-50%); }
            60% { transform: translateY(-10px) translateX(-50%); }
        }

        /* =========================================
           6. SECTIONS & 3D CARDS (Services & Properties)
           ========================================= */
        section {
            padding: 120px 8%;
            position: relative;
        }
        .section-header {
            text-align: center;
            margin-bottom: 80px;
        }
        .section-header h2 {
            font-size: 3rem;
            color: var(--secondary);
            margin-bottom: 15px;
        }
        .section-header p {
            color: var(--grey);
            max-width: 700px;
            margin: 0 auto;
        }

        .grid-3 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            perspective: var(--perspective);
        }

        /* 3D Tilt Card */
        .tilt-card {
            background: var(--glass);
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            padding: 40px 30px;
            border-radius: 15px;
            text-align: center;
            transition: var(--transition);
            transform-style: preserve-3d;
            position: relative;
        }
        .tilt-card:hover {
            border-color: var(--secondary);
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
        }
        .tilt-card i {
            font-size: 3rem;
            color: var(--secondary);
            margin-bottom: 20px;
            transform: translateZ(50px);
        }
        .tilt-card h3 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: var(--light);
            transform: translateZ(30px);
        }
        .tilt-card p {
            color: var(--grey);
            transform: translateZ(20px);
        }

        /* Properties Section */
        .properties-bg { background: #05080f; }
        .property-card {
            background: var(--accent);
            border-radius: 15px;
            overflow: hidden;
            position: relative;
            transition: var(--transition);
            border: 1px solid transparent;
        }
        .property-card:hover {
            transform: translateY(-15px);
            border-color: var(--secondary);
            box-shadow: 0 20px 40px rgba(0,0,0,0.5);
        }
        .property-img {
            height: 300px;
            overflow: hidden;
            position: relative;
        }
        .property-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.8s ease;
        }
        .property-card:hover .property-img img { transform: scale(1.1); }
        .property-badge {
            position: absolute;
            top: 20px;
            right: 20px;
            background: var(--secondary);
            color: var(--primary);
            padding: 8px 20px;
            border-radius: 25px;
            font-weight: 700;
            font-size: 0.9rem;
            z-index: 5;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }
        .property-info { padding: 30px; }
        .property-info h3 {
            font-size: 1.6rem;
            margin-bottom: 10px;
            color: var(--light);
        }
        .property-info p {
            color: var(--grey);
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .property-wa-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            background: transparent;
            border: 2px solid #25D366;
            color: #25D366;
            padding: 12px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 700;
            transition: var(--transition);
            width: 100%;
        }
        .property-wa-btn:hover {
            background: #25D366;
            color: white;
            box-shadow: 0 5px 15px rgba(37, 211, 102, 0.3);
        }

        /* =========================================
           7. STATS COUNTER (Legacy)
           ========================================= */
        .stats-section {
            background: url('https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?q=80&w=2070') no-repeat center center/cover fixed;
            position: relative;
        }
        .stats-overlay {
            background: rgba(8, 11, 20, 0.9);
            padding: 100px 8%;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
            text-align: center;
        }
        .stat-item h2 {
            font-size: 4rem;
            color: var(--secondary);
            margin-bottom: 10px;
        }
        .stat-item p {
            font-size: 1.2rem;
            color: var(--light);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* =========================================
           8. SMART WHATSAPP WIDGET
           ========================================= */
        .wa-widget {
            position: fixed;
            bottom: 40px;
            right: 40px;
            z-index: 999;
        }
        .wa-btn-main {
            width: 70px;
            height: 70px;
            background: #25D366;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            color: white;
            cursor: pointer;
            border: none;
            box-shadow: 0 10px 25px rgba(37, 211, 102, 0.5);
            animation: pulseWa 2s infinite;
            transition: transform 0.3s ease;
        }
        .wa-btn-main:hover { transform: rotate(15deg) scale(1.1); }
        @keyframes pulseWa {
            0% { box-shadow: 0 0 0 0 rgba(37, 211, 102, 0.5); }
            70% { box-shadow: 0 0 0 20px rgba(37, 211, 102, 0); }
            100% { box-shadow: 0 0 0 0 rgba(37, 211, 102, 0); }
        }
        .wa-form-popup {
            position: absolute;
            bottom: 85px;
            right: 0;
            width: 350px;
            background: var(--light);
            color: var(--primary);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 15px 40px rgba(0,0,0,0.3);
            display: none;
            flex-direction: column;
            gap: 15px;
            opacity: 0;
            transform: translateY(20px);
            transition: var(--transition);
        }
        .wa-form-popup.active {
            display: flex;
            opacity: 1;
            transform: translateY(0);
        }
        .wa-form-popup h3 {
            color: var(--primary);
            border-bottom: 2px solid var(--secondary);
            padding-bottom: 10px;
        }
        .wa-form-popup input {
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-family: var(--font-main);
        }
        .wa-form-popup button {
            background: var(--primary);
            color: var(--secondary);
            border: none;
            padding: 12px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 700;
            font-size: 1rem;
            transition: var(--transition);
        }
        .wa-form-popup button:hover { background: #1a2238; }

        /* =========================================
           9. FOOTER
           ========================================= */
        footer {
            background: #02040a;
            padding: 60px 8%;
            text-align: center;
            border-top: 1px solid rgba(212, 175, 55, 0.2);
        }
        footer h2 { color: var(--secondary); margin-bottom: 20px; }
        footer p { color: var(--grey); margin-bottom: 10px; }
        .footer-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }
        .footer-links a {
            color: var(--light);
            font-size: 1.5rem;
            transition: var(--transition);
        }
        .footer-links a:hover { color: var(--secondary); transform: translateY(-5px); }

        /* =========================================
           10. RESPONSIVE DESIGN
           ========================================= */
        @media (max-width: 992px) {
            .nav-links {
                display: none;
                flex-direction: column;
                position: absolute;
                top: 80px;
                right: 0;
                background: rgba(8,11,20,0.98);
                width: 100%;
                padding: 30px;
                gap: 20px;
                border-bottom: 1px solid var(--glass-border);
            }
            .nav-links.active { display: flex; }
            .menu-toggle { display: block; }
            .hero-content h2 { font-size: 3rem; }
            .hero-content p { font-size: 1.1rem; }
            section { padding: 80px 5%; }
            .section-header h2 { font-size: 2.2rem; }
            .wa-widget { bottom: 20px; right: 20px; }
            .wa-btn-main { width: 60px; height: 60px; font-size: 2rem; }
        }
    </style>
</head>
<body data-lang="ar">

    <!-- Preloader -->
    <div id="preloader">
        <div class="loader-logo">RAGIA</div>
        <div class="loader-span">REAL ESTATE</div>
    </div>

    <!-- Flying Logos Background -->
    <div class="bg-logos">
        <div class="floating-logo">RAGIA</div>
        <div class="floating-logo">REAL ESTATE</div>
        <div class="floating-logo">LUXURY</div>
    </div>

    <!-- Header -->
    <header>
        <div class="logo-container" onclick="window.location.href='#home'">
            <h1>RAGIA</h1>
            <span data-ar="العقارية" data-en="Real Estate">العقارية</span>
        </div>
        <ul class="nav-links">
            <li><a href="#home" data-ar="الرئيسية" data-en="Home">الرئيسية</a></li>
            <li><a href="#services" data-ar="خدماتنا" data-en="Services">خدماتنا</a></li>
            <li><a href="#properties" data-ar="عقاراتنا" data-en="Properties">عقاراتنا</a></li>
            <li><a href="#contact" data-ar="تواصل معنا" data-en="Contact">تواصل معنا</a></li>
        </ul>
        <div class="nav-controls">
            <button class="lang-btn" onclick="toggleLanguage()">EN</button>
            <i class="fas fa-bars menu-toggle" onclick="toggleMenu()"></i>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home" style="padding: 0; max-width: 100%;">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <h2 data-ar="راقية للعقارات.. إرث من الثقة" data-en="Ragia Real Estate.. Legacy of Trust">راقية للعقارات.. إرث من الثقة</h2>
            <p data-ar="خبرة تمتد لـ 20 عاماً (منذ 2003) في التسويق العقاري بالسودان، مصر، الإمارات، والسعودية." data-en="20 years of experience (Since 2003) in real estate marketing across Sudan, Egypt, UAE, and Saudi Arabia.">خبرة تمتد لـ 20 عاماً (منذ 2003) في التسويق العقاري بالسودان، مصر، الإمارات، والسعودية.</p>
            <a href="#properties" class="btn-gold" data-ar="استكشف عقاراتنا" data-en="Explore Properties">استكشف عقاراتنا</a>
        </div>
        <a href="#services" class="scroll-down"><i class="fas fa-chevron-down"></i></a>
    </section>

    <!-- Services Section (3D Tilt Cards) -->
    <section id="services">
        <div class="section-header">
            <h2 data-ar="خدماتنا المتميزة" data-en="Our Premium Services">خدماتنا المتميزة</h2>
            <p data-ar="نقدم باقة متكاملة من الخدمات العقارية المصممة لتلبية كافة احتياجات عملائنا." data-en="We offer a comprehensive suite of real estate services tailored to meet all our clients' needs.">نقدم باقة متكاملة من الخدمات العقارية المصممة لتلبية كافة احتياجات عملائنا.</p>
        </div>
        <div class="grid-3">
            <div class="tilt-card">
                <i class="fas fa-building"></i>
                <h3 data-ar="التسويق العقاري" data-en="Real Estate Marketing">التسويق العقاري</h3>
                <p data-ar="تسويق المنازل، الفلل، الشقق، الأبراج، والمزارع باحترافية عالية." data-en="Marketing homes, villas, apartments, towers, and farms with high professionalism.">تسويق المنازل، الفلل، الشقق، الأبراج، والمزارع باحترافية عالية.</p>
            </div>
            <div class="tilt-card">
                <i class="fas fa-hand-holding-usd"></i>
                <h3 data-ar="الاستشارات العقارية" data-en="Real Estate Consultation">الاستشارات العقارية</h3>
                <p data-ar="فريق من الخبراء يقدمون لك استشارات دقيقة لضمان أفضل القرارات الاستثمارية." data-en="A team of experts providing accurate consultations to ensure the best investment decisions.">فريق من الخبراء يقدمون لك استشارات دقيقة لضمان أفضل القرارات الاستثمارية.</p>
            </div>
            <div class="tilt-card">
                <i class="fas fa-tasks"></i>
                <h3 data-ar="إدارة العقارات" data-en="Property Management">إدارة العقارات</h3>
                <p data-ar="نتولى إدارة عقاراتك بالكامل، من الصيانة إلى التأجير، لضمان راحتك." data-en="We manage your properties entirely, from maintenance to renting, ensuring your peace of mind.">نتولى إدارة عقاراتك بالكامل، من الصيانة إلى التأجير، لضمان راحتك.</p>
            </div>
        </div>
    </section>

    <!-- Properties Showcase -->
    <section id="properties" class="properties-bg">
        <div class="section-header">
            <h2 data-ar="مختارات من عقاراتنا" data-en="Selected Properties">مختارات من عقاراتنا</h2>
            <p data-ar="عقارات حصرية في أرقى المناطق، جاهزة لتلبية تطلعاتك السكنية والاستثمارية." data-en="Exclusive properties in prime locations, ready to meet your residential and investment aspirations.">عقارات حصرية في أرقى المناطق، جاهزة لتلبية تطلعاتك السكنية والاستثمارية.</p>
        </div>
        <div class="grid-3" id="propertiesGrid">
            <!-- Properties injected by JS -->
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats-section" style="padding: 0;">
        <div class="stats-overlay">
            <div class="stats-grid">
                <div class="stat-item">
                    <h2 class="counter" data-target="20">0</h2>
                    <p data-ar="سنة من الخبرة" data-en="Years of Experience">سنة من الخبرة</p>
                </div>
                <div class="stat-item">
                    <h2 class="counter" data-target="1500">0</h2>
                    <p data-ar="عقار تم بيعه" data-en="Properties Sold">عقار تم بيعه</p>
                </div>
                <div class="stat-item">
                    <h2 class="counter" data-target="4">0</h2>
                    <p data-ar="دول نشغل فيها" data-en="Countries Operating">دول نشغل فيها</p>
                </div>
                <div class="stat-item">
                    <h2 class="counter" data-target="100" data-suffix="%">0</h2>
                    <p data-ar="رضا العملاء" data-en="Client Satisfaction">رضا العملاء</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contact">
        <h2>RAGIA REAL ESTATE</h2>
        <p data-ar="الخرطوم - السودان | مصر | الإمارات | السعودية" data-en="Khartoum - Sudan | Egypt | UAE | Saudi Arabia">الخرطوم - السودان | مصر | الإمارات | السعودية</p>
        <p>📞 <a href="tel:+249912339585" style="color: var(--secondary); text-decoration: none;">+249912339585</a> | 💬 <a href="https://wa.me/249920119571" style="color: var(--secondary); text-decoration: none;">+249920119571</a></p>
        <div class="footer-links">
            <a href="#"><i class="fab fa-facebook-f"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-whatsapp"></i></a>
            <a href="#"><i class="fab fa-linkedin-in"></i></a>
        </div>
    </footer>

    <!-- Smart WhatsApp Widget -->
    <div class="wa-widget">
        <div class="wa-form-popup" id="waForm">
            <h3 data-ar="تواصل مباشر معنا" data-en="Contact Us Directly">تواصل مباشر معنا</h3>
            <input type="text" id="clientName" data-ar-placeholder="اسمك الكريم" data-en-placeholder="Your Name" placeholder="اسمك الكريم">
            <input type="text" id="msgSubject" data-ar-placeholder="موضوع الرسالة" data-en-placeholder="Message Subject" placeholder="موضوع الرسالة">
            <button onclick="sendGeneralInquiry()" data-ar="إرسال عبر واتساب" data-en="Send via WhatsApp">إرسال عبر واتساب <i class="fab fa-whatsapp"></i></button>
        </div>
        <button class="wa-btn-main" onclick="toggleWaForm()"><i class="fab fa-whatsapp"></i></button>
    </div>

    <script>
        /* =========================================
           1. PRELOADER & NAVBAR SCROLL
           ========================================= */
        window.addEventListener('load', () => {
            document.getElementById('preloader').classList.add('hidden');
        });

        window.addEventListener('scroll', () => {
            const header = document.querySelector('header');
            if (window.scrollY > 50) header.classList.add('scrolled');
            else header.classList.remove('scrolled');
        });

        function toggleMenu() {
            document.querySelector('.nav-links').classList.toggle('active');
        }

        /* =========================================
           2. LANGUAGE SWITCHING (AR / EN)
           ========================================= */
        function toggleLanguage() {
            const body = document.body;
            const currentLang = body.getAttribute('data-lang');
            const newLang = currentLang === 'ar' ? 'en' : 'ar';
            body.setAttribute('data-lang', newLang);
            body.setAttribute('dir', newLang === 'ar' ? 'rtl' : 'ltr');
            body.setAttribute('lang', newLang);

            document.querySelectorAll('[data-ar]').forEach(el => {
                el.innerText = el.getAttribute(`data-${newLang}`);
            });

            document.querySelectorAll('[data-ar-placeholder]').forEach(el => {
                el.setAttribute('placeholder', el.getAttribute(`data-${newLang}-placeholder`));
            });

            document.querySelector('.lang-btn').innerText = newLang === 'ar' ? 'EN' : 'عربي';
            renderProperties(); // Re-render properties to update language
        }

        /* =========================================
           3. SMART WHATSAPP WIDGET
           ========================================= */
        const waNumber = "249920119571";

        function toggleWaForm() {
            document.getElementById('waForm').classList.toggle('active');
        }

        function sendGeneralInquiry() {
            const name = document.getElementById('clientName').value;
            const subject = document.getElementById('msgSubject').value;
            const isAr = document.body.getAttribute('data-lang') === 'ar';
            
            let msg = isAr ? "مرحباً راقية للعقارات،\n" : "Hello Ragia Real Estate,\n";
            if (name) msg += (isAr ? `اسمي: ${name}\n` : `My name is: ${name}\n`);
            if (subject) msg += (isAr ? `أبحث عن: ${subject}\n` : `I am looking for: ${subject}\n`);
            msg += (isAr ? "أرجو التواصل معي." : "Please contact me.");
            
            window.open(`https://wa.me/${waNumber}?text=${encodeURIComponent(msg)}`, '_blank');
        }

        function askAboutProperty(title, location) {
            const isAr = document.body.getAttribute('data-lang') === 'ar';
            const msg = isAr 
                ? `مرحباً راقية للعقارات، أنا مهتم بالعقار التالي:\n*${title}*\nالموقع: ${location}\nأرجو تزويدي بالتفاصيل والسعر.`
                : `Hello Ragia Real Estate, I am interested in the following property:\n*${title}*\nLocation: ${location}\nPlease provide details and price.`;
            
            window.open(`https://wa.me/${waNumber}?text=${encodeURIComponent(msg)}`, '_blank');
        }

        /* =========================================
           4. DYNAMIC PROPERTIES (Mock Data)
           ========================================= */
        const propertiesData = [
            {
                img: "https://images.unsplash.com/photo-1613490493576-7fde63acd811?q=80&w=2070",
                type: { ar: "فلل", en: "Villa" },
                title: { ar: "قصر فاخر - الخرطوم", en: "Luxury Palace - Khartoum" },
                loc: { ar: "الخرطوم - حي الراقية", en: "Khartoum - Al Raqya" },
                price: { ar: "بسعر يطلب", en: "Price on Request" }
            },
            {
                img: "https://images.unsplash.com/photo-1502672260266-1c1ef2d93688?q=80&w=2000",
                type: { ar: "شقق", en: "Apartment" },
                title: { ar: "شقة تمليك فخمة - مصر", en: "Luxury Apartment - Egypt" },
                loc: { ar: "القاهرة - التجمع الخامس", en: "Cairo - New Cairo" },
                price: { ar: "15 مليون جنيه", en: "15M EGP" }
            },
            {
                img: "https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?q=80&w=2070",
                type: { ar: "أبراج", en: "Tower" },
                title: { ar: "برج استثماري - الإمارات", en: "Investment Tower - UAE" },
                loc: { ar: "دبي - وسط المدينة", en: "Dubai - Downtown" },
                price: { ar: "استثمار", en: "Investment" }
            }
        ];

        function renderProperties() {
            const grid = document.getElementById('propertiesGrid');
            const lang = document.body.getAttribute('data-lang');
            grid.innerHTML = '';
            
            propertiesData.forEach(p => {
                const title = p.title[lang];
                const loc = p.loc[lang];
                grid.innerHTML += `
                    <div class="property-card">
                        <div class="property-img">
                            <span class="property-badge">${p.type[lang]}</span>
                            <img src="${p.img}" alt="${title}">
                        </div>
                        <div class="property-info">
                            <h3>${title}</h3>
                            <p><i class="fas fa-map-marker-alt"></i> ${loc} | <i class="fas fa-tag"></i> ${p.price[lang]}</p>
                            <button class="property-wa-btn" onclick="askAboutProperty('${title}', '${loc}')">
                                <i class="fab fa-whatsapp"></i> ${lang === 'ar' ? 'استفسر الآن' : 'Inquire Now'}
                            </button>
                        </div>
                    </div>
                `;
            });
        }
        renderProperties(); // Initial render

        /* =========================================
           5. 3D TILT EFFECT ON CARDS (Mouse Tracking)
           ========================================= */
        document.querySelectorAll('.tilt-card').forEach(card => {
            card.addEventListener('mousemove', (e) => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                
                const rotateX = (y - centerY) / 10;
                const rotateY = (centerX - x) / 10;
                
                card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.05, 1.05, 1.05)`;
            });
            
            card.addEventListener('mouseleave', () => {
                card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) scale3d(1, 1, 1)';
            });
        });

        /* =========================================
           6. STATS COUNTER ANIMATION
           ========================================= */
        const counters = document.querySelectorAll('.counter');
        const speed = 200;

        const startCounting = (counter) => {
            const target = +counter.getAttribute('data-target');
            const suffix = counter.getAttribute('data-suffix') || '';
            let current = 0;

            const updateCount = () => {
                const inc = target / speed;
                if (current < target) {
                    current += inc;
                    counter.innerText = Math.ceil(current) + suffix;
                    setTimeout(updateCount, 10);
                } else {
                    counter.innerText = target + suffix;
                }
            };
            updateCount();
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    startCounting(entry.target);
                    observer.unobserve(entry.target);
                }
            });
        });

        counters.forEach(c => observer.observe(c));
    </script>
</body>
</html>